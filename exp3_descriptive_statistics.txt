# descriptive statistics – measures of central tendency and variability

---

# cell 1 – installing required libraries

# install required python libraries

# pandas -> data manipulation
# numpy -> numerical calculations
# matplotlib -> plotting graphs
# seaborn -> advanced visualization

# run only once if libraries are not installed

# !pip install pandas numpy matplotlib seaborn scikit-learn


---

# cell 2 – importing required libraries

# importing required python libraries

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns


---

# cell 3 – loading iris dataset using url

# loading iris dataset directly using url

url = "https://raw.githubusercontent.com/uiuc-cse/data-fa14/gh-pages/data/iris.csv"

# reading dataset

df = pd.read_csv(url)

# displaying first 5 rows

print(df.head())




---

# cell 4 – checking dataset dimensions

# checking number of rows and columns

print("shape of dataset:", df.shape)


---

# cell 5 – dataset information

# displaying dataset information

print(df.info())


---

# cell 6 – checking missing values

# checking missing values in dataset

print(df.isnull().sum())


---

# cell 7 – descriptive statistics of complete dataset

# generating descriptive statistics

print(df.describe())

# describe() gives:
# mean
# standard deviation
# minimum value
# maximum value
# quartiles


---

# cell 8 – checking unique species values

# displaying unique species categories

print(df['species'].unique())


---

# cell 9 – grouping data by species

# grouping dataset by species

grouped_data = df.groupby('species')

# displaying grouped object

print(grouped_data)   # group object stored in memory
print(grouped_data.first())
print(grouped_data.mean())



---

# cell 10 – mean of numeric variables grouped by species

# calculating mean grouped by species

print(grouped_data.mean(numeric_only=True))


---

# cell 11 – median grouped by species

# calculating median grouped by species

print(grouped_data.median(numeric_only=True))


---

# cell 12 – minimum values grouped by species

# calculating minimum values grouped by species

print(grouped_data.min(numeric_only=True))


---

# cell 13 – maximum values grouped by species

# calculating maximum values grouped by species

print(grouped_data.max(numeric_only=True))


---

# cell 14 – standard deviation grouped by species

# calculating standard deviation grouped by species

print(grouped_data.std(numeric_only=True))


---

# cell 15 – combined summary statistics grouped by species

# displaying multiple summary statistics together

summary_statistics = grouped_data.agg([
    'mean',
    'median',
    'min',
    'max',
    'std'
])

print(summary_statistics)


---

# cell 16 – creating list for each species

# creating separate lists for sepal length

setosa_list = df[
    df['species'] == 'setosa'
]['sepal_length'].tolist()

versicolor_list = df[
    df['species'] == 'versicolor'
]['sepal_length'].tolist()

virginica_list = df[
    df['species'] == 'virginica'
]['sepal_length'].tolist()

# displaying lists

print("setosa sepal length list:")
print(setosa_list)

print("\nversicolor sepal length list:")
print(versicolor_list)

print("\nvirginica sepal length list:")
print(virginica_list)


---

# cell 17 – percentile calculation for iris setosa

# filtering iris setosa data

setosa = df[
    df['species'] == 'setosa'
]

# calculating percentiles

print(np.percentile(setosa['sepal_length'], 25))
print(np.percentile(setosa['sepal_length'], 50))
print(np.percentile(setosa['sepal_length'], 75))


---

# cell 18 – percentile calculation for iris versicolor

# filtering iris versicolor data

versicolor = df[
    df['species'] == 'versicolor'
]

# calculating percentiles

print(np.percentile(versicolor['sepal_length'], 25))
print(np.percentile(versicolor['sepal_length'], 50))
print(np.percentile(versicolor['sepal_length'], 75))


---

# cell 19 – percentile calculation for iris virginica

# filtering iris virginica data

virginica = df[
    df['species'] == 'virginica'
]

# calculating percentiles

print(np.percentile(virginica['sepal_length'], 25))
print(np.percentile(virginica['sepal_length'], 50))
print(np.percentile(virginica['sepal_length'], 75))


---

# cell 20 – detailed statistics for iris setosa

# displaying statistics for iris setosa

print("statistics for iris setosa")

print(setosa.describe())


---

# cell 21 – detailed statistics for iris versicolor

# displaying statistics for iris versicolor

print("statistics for iris versicolor")

print(versicolor.describe())


---

# cell 22 – detailed statistics for iris virginica

# displaying statistics for iris virginica

print("statistics for iris virginica")

print(virginica.describe())


---

# cell 23 – histogram using matplotlib

# histogram using matplotlib

plt.figure(figsize=(6,4))

plt.hist(df['sepal_length'])

plt.title("sepal length distribution using matplotlib")
plt.xlabel("sepal length")
plt.ylabel("frequency")

plt.show()


---

# cell 24 – histogram using seaborn

# histogram using seaborn

plt.figure(figsize=(6,4))

sns.histplot(df['sepal_length'])

plt.title("sepal length distribution using seaborn")
plt.xlabel("sepal length")
plt.ylabel("frequency")

plt.show()


---

# cell 25 – boxplot using matplotlib

# boxplot using matplotlib

plt.figure(figsize=(6,4))

plt.boxplot(df['petal_length'])

plt.title("petal length boxplot using matplotlib")

plt.show()


---

# cell 26 – boxplot using seaborn

# boxplot using seaborn

plt.figure(figsize=(6,4))

sns.boxplot(y=df['petal_length'])

plt.title("petal length boxplot using seaborn")

plt.show()


---

# cell 27 – scatter plot using matplotlib

# scatter plot using matplotlib

plt.figure(figsize=(6,4))

plt.scatter(
    df['sepal_length'],
    df['petal_length']
)

plt.title("sepal length vs petal length using matplotlib")
plt.xlabel("sepal length")
plt.ylabel("petal length")

plt.show()


---

# cell 28 – scatter plot using seaborn

# scatter plot using seaborn

plt.figure(figsize=(6,4))

sns.scatterplot(
    x='sepal_length',
    y='petal_length',
    hue='species',
    data=df
)

plt.title("sepal length vs petal length using seaborn")

plt.show()


---

# cell 29 – correlation heatmap

# plotting correlation heatmap

plt.figure(figsize=(8,5))

sns.heatmap(
    df.corr(numeric_only=True),
    annot=True
)

plt.title("correlation heatmap")

plt.show()


---

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# writeup

# aim

perform the following operations on any open source dataset using python:

1. provide summary statistics (mean, median, minimum, maximum, standard deviation) for a dataset with numeric variables grouped by one categorical variable. create a list that contains a numeric value for each response to the categorical variable.
2. write a python program to display basic statistical details like percentile, mean, standard deviation etc. of the species of iris dataset.

---

# dataset information

## dataset used

Iris Dataset

## dataset source

[Iris Dataset CSV Source](https://raw.githubusercontent.com/uiuc-cse/data-fa14/gh-pages/data/iris.csv)

---

# theory

# descriptive statistics

descriptive statistics are statistical methods used to summarize and describe important characteristics of data.

they help in:

* understanding dataset distribution
* identifying patterns
* comparing groups
* summarizing numerical data

---

# measures of central tendency

measures of central tendency represent the central or average value of dataset.

types:

* mean
* median
* mode

---

# mean

mean is the arithmetic average of values.

formula:

Mean = (∑x)/n

where:

* x = observations
* n = total number of observations

---

# median

median is the middle value when data is arranged in order.

if number of observations is even:
median is average of two middle values.

---

# minimum and maximum

* minimum = smallest value
* maximum = largest value

these help identify range of dataset.

---

# standard deviation

standard deviation measures spread of data around mean.

small standard deviation:

* values are close to mean

large standard deviation:

* values are spread out

formula:

σ= sqrt((∑(x−μ)^2​)/n)
​

---

# percentile

percentile indicates percentage of observations below a given value.

examples:

* 25th percentile
* 50th percentile
* 75th percentile

50th percentile is also called median.

---

# grouping data

grouping means dividing dataset into categories.

in this practical:

* dataset was grouped by species column.

species categories:

* setosa
* versicolor
* virginica

---

# iris dataset

the iris dataset contains flower measurements:

* sepal length
* sepal width
* petal length
* petal width

used for:

* statistical analysis
* machine learning
* classification problems

---

# libraries used

| library    | purpose                |
| ---------- | ---------------------- |
| pandas     | data manipulation      |
| numpy      | numerical calculations |
| matplotlib | plotting graphs        |
| seaborn    | advanced visualization |

---

# important functions used

| function        | purpose                       |
| --------------- | ----------------------------- |
| `read_csv()`    | loads dataset                 |
| `describe()`    | statistical summary           |
| `groupby()`     | groups data                   |
| `mean()`        | calculates mean               |
| `median()`      | calculates median             |
| `std()`         | calculates standard deviation |
| `percentile()`  | calculates percentiles        |
| `histplot()`    | plots histogram               |
| `scatterplot()` | plots scatter graph           |

---


# conclusion

thus, descriptive statistical analysis was successfully performed on the iris dataset using python.

different measures of central tendency and variability such as:

* mean
* median
* minimum
* maximum
* standard deviation
* percentile

were calculated for different species of iris flowers.

graphs and visualizations helped in better understanding of data distribution and relationships between variables.


----------------------------------------------------------------------------------------------------------------------------------------------------------------------
correct loading code for downloaded dataset
# loading downloaded iris dataset

df = pd.read_csv("Iris.csv")

# displaying first 5 rows

print(df.head())
then these lines must change
old code
df['species']
new code
df['Species']

because column name starts with capital S.

old code
df['sepal_length']
new code
df['SepalLengthCm']
important changes you must make everywhere
old column name	new column name
species	       Species
sepal_length	SepalLengthCm
sepal_width	SepalWidthCm
petal_length	PetalLengthCm
petal_width	PetalWidthCm
also there is one extra column
Id

this is just serial number.

not useful for analysis.

better to remove it.

add this cell after loading dataset
# removing unnecessary id column

df.drop('Id', axis=1, inplace=True)

# displaying dataset

print(df.head())
then grouping code becomes
old
grouped_data = df.groupby('species')
new
grouped_data = df.groupby('Species')
filtering setosa changes too
old
setosa = df[df['species'] == 'setosa']
new
setosa = df[df['Species'] == 'Iris-setosa']
similarly
versicolor = df[df['Species'] == 'Iris-versicolor']

virginica = df[df['Species'] == 'Iris-virginica']
histogram changes too
old
plt.hist(df['sepal_length'])
new
plt.hist(df['SepalLengthCm'])
scatter plot changes
old
sns.scatterplot(
    x='sepal_length',
    y='petal_length',
    hue='species',
    data=df
)
new
sns.scatterplot(
    x='SepalLengthCm',
    y='PetalLengthCm',
    hue='Species',
    data=df
)


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# using downloaded dataset

# cell 1 – installing required libraries

# install required python libraries

# pandas -> data manipulation
# numpy -> numerical calculations
# matplotlib -> plotting graphs
# seaborn -> advanced visualization

# run only once if libraries are not installed

# !pip install pandas numpy matplotlib seaborn scikit-learn


---


# cell 2 – importing required libraries

# importing required python libraries

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns


---


# cell 3 – loading iris dataset from downloaded csv file

# loading dataset
# https://www.kaggle.com/datasets/uciml/iris/data

df = pd.read_csv("Iris.csv")

# displaying first 5 rows

print(df.head())


---


# cell 4 – checking dataset dimensions

# checking number of rows and columns

print("shape of dataset:", df.shape)


---


# cell 5 – dataset information

# displaying dataset information

print(df.info())


---


# cell 6 – checking missing values

# checking missing values in dataset

print(df.isnull().sum())


---


# cell 7 – descriptive statistics of complete dataset

# generating descriptive statistics

print(df.describe())

# describe() gives:
# mean
# standard deviation
# minimum value
# maximum value
# quartiles


---


# cell 8 – checking unique species values

# displaying unique species categories

print(df['Species'].unique())


---


# cell 9 – grouping data by species

# grouping dataset by species

grouped_data = df.groupby('Species')

# displaying grouped object

print(grouped_data)

print(grouped_data.first())

print(grouped_data.mean(numeric_only=True))

---


# cell 10 – mean of numeric variables grouped by species

# calculating mean grouped by species

print(grouped_data.mean(numeric_only=True))


---


# cell 11 – median grouped by species

# calculating median grouped by species

print(grouped_data.median(numeric_only=True))


---


# cell 12 – minimum values grouped by species

# calculating minimum values grouped by species

print(grouped_data.min(numeric_only=True))


---


# cell 13 – maximum values grouped by species

# calculating maximum values grouped by species

print(grouped_data.max(numeric_only=True))


---


# cell 14 – standard deviation grouped by species

# calculating standard deviation grouped by species

print(grouped_data.std(numeric_only=True))


---


# cell 15 – combined summary statistics grouped by species

# displaying multiple summary statistics together

summary_statistics = grouped_data.agg([
    'mean',
    'median',
    'min',
    'max',
    'std'
])

print(summary_statistics)


---


# cell 16 – creating list for each species

# creating separate lists for sepal length

setosa_list = df[
    df['Species'] == 'Iris-setosa'
]['SepalLengthCm'].tolist()

versicolor_list = df[
    df['Species'] == 'Iris-versicolor'
]['SepalLengthCm'].tolist()

virginica_list = df[
    df['Species'] == 'Iris-virginica'
]['SepalLengthCm'].tolist()

# displaying lists

print("setosa sepal length list:")
print(setosa_list)

print("\nversicolor sepal length list:")
print(versicolor_list)

print("\nvirginica sepal length list:")
print(virginica_list)


---


# cell 17 – percentile calculation for iris setosa

# filtering iris setosa data

setosa = df[
    df['Species'] == 'Iris-setosa'
]

# calculating percentiles

print(np.percentile(setosa['SepalLengthCm'], 25))

print(np.percentile(setosa['SepalLengthCm'], 50))

print(np.percentile(setosa['SepalLengthCm'], 75))


---


# cell 18 – percentile calculation for iris versicolor

# filtering iris versicolor data

versicolor = df[
    df['Species'] == 'Iris-versicolor'
]

# calculating percentiles

print(np.percentile(versicolor['SepalLengthCm'], 25))

print(np.percentile(versicolor['SepalLengthCm'], 50))

print(np.percentile(versicolor['SepalLengthCm'], 75))


---

# cell 19 – percentile calculation for iris virginica

# filtering iris virginica data

virginica = df[
    df['Species'] == 'Iris-virginica'
]

# calculating percentiles

print(np.percentile(virginica['SepalLengthCm'], 25))

print(np.percentile(virginica['SepalLengthCm'], 50))

print(np.percentile(virginica['SepalLengthCm'], 75))


---


# cell 20 – detailed statistics for iris setosa

# displaying statistics for iris setosa

print("statistics for iris setosa")

print(setosa.describe())


---

# cell 21 – detailed statistics for iris versicolor

# displaying statistics for iris versicolor

print("statistics for iris versicolor")

print(versicolor.describe())


---


# cell 22 – detailed statistics for iris virginica

# displaying statistics for iris virginica

print("statistics for iris virginica")

print(virginica.describe())


---


# cell 23 – histogram using matplotlib

# histogram using matplotlib

plt.figure(figsize=(6,4))

plt.hist(df['SepalLengthCm'])

plt.title("sepal length distribution using matplotlib")
plt.xlabel("sepal length")
plt.ylabel("frequency")

plt.show()


---


# cell 24 – histogram using seaborn

# histogram using seaborn

plt.figure(figsize=(6,4))

sns.histplot(df['SepalLengthCm'])

plt.title("sepal length distribution using seaborn")
plt.xlabel("sepal length")
plt.ylabel("frequency")

plt.show()


---

# cell 25 – boxplot using matplotlib

# boxplot using matplotlib

plt.figure(figsize=(6,4))

plt.boxplot(df['PetalLengthCm'])

plt.title("petal length boxplot using matplotlib")

plt.show()


---


# cell 26 – boxplot using seaborn

# boxplot using seaborn

plt.figure(figsize=(6,4))

sns.boxplot(y=df['PetalLengthCm'])

plt.title("petal length boxplot using seaborn")

plt.show()


---


# cell 27 – scatter plot using matplotlib

# scatter plot using matplotlib

plt.figure(figsize=(6,4))

plt.scatter(
    df['SepalLengthCm'],
    df['PetalLengthCm']
)

plt.title("sepal length vs petal length using matplotlib")
plt.xlabel("sepal length")
plt.ylabel("petal length")

plt.show()


---


# cell 28 – scatter plot using seaborn

# scatter plot using seaborn

plt.figure(figsize=(6,4))

sns.scatterplot(
    x='SepalLengthCm',
    y='PetalLengthCm',
    hue='Species',
    data=df
)

plt.title("sepal length vs petal length using seaborn")

plt.show()


---


# cell 29 – correlation heatmap

# plotting correlation heatmap

plt.figure(figsize=(8,5))

sns.heatmap(
    df.corr(numeric_only=True),
    annot=True
)

plt.title("correlation heatmap")

plt.show()


